[project]
name = "dbt-data-quality"
version = "0.1.0"
description = "dbt project for the dbt x Elementary data quality demo"
requires-python = ">=3.13"
dependencies = [
    "dbt-snowflake==1.12.0",
    "elementary-data[databricks]==0.25.1",
]

[tool.uv]
package = false
